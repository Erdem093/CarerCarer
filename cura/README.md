# Cura

Flutter app for voice check-ins, letter explanations, and caregiver support.

## Backend

The Twilio calling flow requires the Node backend in [backend](/Users/erdem/Projects/App_Tests/cura/backend).

For hosted deployment instructions, see:

- [DEPLOY.md](/Users/erdem/Projects/App_Tests/cura/backend/DEPLOY.md)

Once the backend is deployed, set `BACKEND_URL` in [`.env`](/Users/erdem/Projects/App_Tests/cura/.env) to the hosted URL so real devices can reach it.
